# iOS-Port M4.3 — Quest-Pin-Tap → Selektion + Highlight — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tippen auf einen Quest-Pin der iOS-Karte wählt ihn aus (Highlight + Geometrie-Umriss + Kamerafokus, andere Pins ausgeblendet), Cluster-Tap zoomt rein, Tap-daneben deselektiert — getreue Übersetzung von Androids `MainActivity.showQuestDetails()`-Selektionssequenz **minus** dem upstream-blockierten Antwort-Formular.

**Architecture:** Selektions-State lebt im bestehenden commonMain-`MapViewModel` als ein `@Stable QuestSelection`-Holder. `MapScreen` wird zur Orchestrierungs-Schicht (wie Androids `MainActivity`): baut die Tap-/Deselect-Handler und den Kamera-Fokus/Restore-Lifecycle. `Map.kt` bekommt die Selektion + Klick-Handler durchgereicht und blendet `PinsLayers` bei Selektion aus, während `SelectedPinsLayer` (oben) den gewählten Pin zeigt und `FocusedGeometryLayers` die Geometrie umrandet. Das Formular-Sheet bleibt eine `// TODO`-Naht.

**Tech Stack:** Kotlin Multiplatform (commonMain), Compose Multiplatform 1.11.1, maplibre-compose `0.13.0-sc1` (lokaler Fork in mavenLocal), Koin, Kotlin 2.4.0, Gradle 8.14 / JDK 21, iOS-Simulator/Gerät (iPhone 16 Pro).

## Global Constraints

- **Verifikationsmodell (kein Unit-TDD):** Test-Sourcesets kompilieren auf diesem WIP-Branch nicht (commonTest/Android rot, upstream-bedingt). Verifikation pro Task = **Link-Gate `./gradlew :app:linkDebugFrameworkIosSimulatorArm64` GRÜN** + (bei sichtbaren Tasks) Simulator-Screenshot via synthetischer Selektions-Injektion + finale Geräte-Bestätigung. Keine erfundenen Unit-Tests.
- **maplibre-compose 0.13.0 crasht zur LAUFZEIT** (nicht beim Linken) bei negativem `iconPadding`/`PaddingValues.Absolute` → jeder neue Icon-Layer klemmt Padding auf ≥0. `iconOffset` darf negativ sein (Offset ≠ Padding).
- **Quest-ANTWORT-/Commit-Pfad NICHT anfassen** (`OsmQuestFormContainer`-`TODO()`s) — nur eine `// TODO`-Naht setzen.
- **Klick-Disziplin:** `ClickResult.Consume` bei Pin-/Cluster-Treffer (sonst propagiert der Klick zum `onMapClick`=Deselect); `onMapClick` nur `Consume` wenn es wirklich deselektiert, sonst `Pass`.
- **Klick-Callbacks laufen auf dem Main-Thread** → DB-Read (`visibleQuestsSource.get`) NIE im Handler blockieren, immer auf `viewModelScope` + `Dispatchers.IO` auslagern; Handler enqueued nur und gibt `ClickResult` synchron zurück.
- **Ein Commit pro Task.** Direkt auf `master` (Fork-Konvention; kein separater Branch). Nicht pushen (sammelt der Orchestrator am Ende; Push nach Nutzer-OK — hier bereits erteilt).
- maplibre-compose-Layer (`SymbolLayer`/`CircleLayer`) haben `visible: Boolean = true` → Pins ausblenden per Visibility-Toggle, **nicht** durch Entfernen aus der Composition (kein Source-Rebuild).

---

## File Structure

- **Neu** `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/QuestSelection.kt` — der `@Stable`-Holder für die aktuelle Selektion.
- **Ändern** `.../screens/main/map/MapViewModel.kt` — Selektions-State + `selectQuest`/`clearSelection`.
- **Ändern** `.../screens/main/map2/layers/PinsLayers.kt` — optionaler `visible: Boolean = true`-Param.
- **Ändern** `.../screens/main/map2/Map.kt` — neue Params (`selectedQuest`, `onClickPin`, `onClickCluster`, `onMapClick`), `SelectedPinsLayer` einbinden, `PinsLayers` bei Selektion ausblenden, `onMapClick` an `MaplibreMap`.
- **Ändern** `.../screens/main/map/MapScreen.kt` — Tap-/Cluster-/Deselect-Handler, Kamera-Fokus/Restore-Lifecycle, Persist-Gate, `focusedGeometry`-Vorrang, Seam-Marker.

Unverändert (nur konsumiert): `SelectedPinsLayer.kt`, `FocusedGeometryLayers.kt`, `QuestPinsManager.kt` (`getQuestKey`), `PinImage.kt` (`pinImageName()`), `GeometryUtils.kt` (`toGeoJsonBoundingBox`), `VisibleQuestsSource` (`get`).

---

### Task 1: `QuestSelection`-Holder

**Files:**
- Create: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/QuestSelection.kt`

**Interfaces:**
- Consumes: `QuestKey` (`data.quest.QuestKey`), `LatLon` (`data.osm.mapdata.LatLon`), `ElementGeometry` (`data.osm.geometry.ElementGeometry`).
- Produces: `data class QuestSelection(questKey: QuestKey, icon: String, markerLocations: List<LatLon>, geometry: ElementGeometry)`.

- [ ] **Step 1: Datei anlegen**

```kotlin
package de.westnordost.streetcomplete.screens.main.map

import androidx.compose.runtime.Stable
import de.westnordost.streetcomplete.data.osm.geometry.ElementGeometry
import de.westnordost.streetcomplete.data.osm.mapdata.LatLon
import de.westnordost.streetcomplete.data.quest.QuestKey

/** The currently selected quest on the map: enough to highlight its pin(s) ([icon] + [markerLocations]),
 *  outline + focus its [geometry], and identify it ([questKey]). Display-only — the answer form is a
 *  separate, upstream-blocked concern. Mirrors what Android's MainActivity.showQuestDetails() needs. */
@Stable
data class QuestSelection(
    val questKey: QuestKey,
    val icon: String,
    val markerLocations: List<LatLon>,
    val geometry: ElementGeometry,
)
```

- [ ] **Step 2: Link-Gate**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 3: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/QuestSelection.kt
git commit -m "M4.3: QuestSelection holder (commonMain)"
```

---

### Task 2: `MapViewModel` — Selektions-State

**Files:**
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapViewModel.kt`

**Interfaces:**
- Consumes: `QuestSelection` (Task 1); existing `visibleQuestsSource: VisibleQuestsSource` (Konstruktor-Arg 6), `viewModelScope`, `_isFollowing`/`setFollowing`, `getQuestKey` (`:172`). `VisibleQuestsSource.get(questKey): Quest?`; `quest.type.icon.pinImageName()` (`map2.PinImage`); `quest.markerLocations`, `quest.geometry`.
- Produces: `val selectedQuest: StateFlow<QuestSelection?>`, `fun selectQuest(questKey: QuestKey)`, `fun clearSelection()`.

- [ ] **Step 1: Imports ergänzen**

Am Importblock von `MapViewModel.kt` hinzufügen:

```kotlin
import de.westnordost.streetcomplete.screens.main.map2.pinImageName
```

(`Dispatchers`, `IO`, `withContext`, `launch`, `MutableStateFlow`, `StateFlow`, `asStateFlow`, `QuestKey`, `viewModelScope` sind bereits importiert. `QuestSelection` liegt im selben Package — kein Import.)

- [ ] **Step 2: State + Funktionen einfügen**

Direkt nach dem `pins`-Block (`MapViewModel.kt:57`, vor `private val viewport`) einfügen:

```kotlin
    private val _selectedQuest = MutableStateFlow<QuestSelection?>(null)
    /** The currently tapped/selected quest, or null. Drives SelectedPinsLayer + geometry focus. */
    val selectedQuest: StateFlow<QuestSelection?> = _selectedQuest.asStateFlow()
```

Und als neue Methoden (z. B. direkt nach `fun getQuestKey(...)`, `:172`):

```kotlin
    /** Select the quest with [questKey]: load it off the main thread, build the selection holder.
     *  No-op if the same quest is already selected (avoids re-running the highlight bounce + camera
     *  focus when the already-selected pin is tapped). Drops follow mode so the follow animation
     *  doesn't fight the focus animation (mirrors Android freezing the map while a quest is open). */
    fun selectQuest(questKey: QuestKey) {
        if (_selectedQuest.value?.questKey == questKey) return
        viewModelScope.launch {
            val quest = withContext(Dispatchers.IO) { visibleQuestsSource.get(questKey) }
            if (quest == null) {
                _selectedQuest.value = null
                return@launch
            }
            _selectedQuest.value = QuestSelection(
                questKey = questKey,
                icon = quest.type.icon.pinImageName(),
                markerLocations = quest.markerLocations.toList(),
                geometry = quest.geometry,
            )
            if (_isFollowing.value) setFollowing(false)
        }
    }

    fun clearSelection() { _selectedQuest.value = null }
```

- [ ] **Step 3: `onCleared` aufräumt nichts Zusätzliches** — `_selectedQuest` ist reiner State, kein Listener/Job; keine Änderung an `onCleared()` nötig. (Bewusst verifizieren, nicht überspringen.)

- [ ] **Step 4: Link-Gate**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`. (Falls `visibleQuestsSource.get` eine andere Signatur hat als `get(QuestKey): Quest?`, hier korrigieren — der Spec nennt `VisibleQuestsSource.kt:153` als Beleg.)

- [ ] **Step 5: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapViewModel.kt
git commit -m "M4.3: MapViewModel selection state (selectQuest/clearSelection)"
```

---

### Task 3: `PinsLayers.visible` + `Map.kt`-Verdrahtung

**Files:**
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map2/layers/PinsLayers.kt`
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map2/Map.kt`

**Interfaces:**
- Consumes: `QuestSelection` (Task 1); `SelectedPinsLayer(icon: String, pinPositions: Collection<LatLon>)`; `FeaturesClickHandler` (`org.maplibre.compose.util`); `MapClickHandler`/`ClickResult` (`org.maplibre.compose.util`).
- Produces: erweiterte `Map(...)`-Signatur mit `selectedQuest`, `onClickPin`, `onClickCluster`, `onMapClick`; `PinsLayers(..., visible: Boolean = true)`.

- [ ] **Step 1: `PinsLayers` um `visible` erweitern**

In `PinsLayers.kt` die Signatur (`:47-51`) ändern:

```kotlin
fun PinsLayers(
    pins: Collection<Pin>,
    onClickPin: FeaturesClickHandler? = null,
    onClickCluster: FeaturesClickHandler? = null,
    visible: Boolean = true,
) {
```

Und an allen drei Layern `visible = visible` ergänzen: dem `pin-cluster-layer`-`SymbolLayer` (`:62`), dem `pin-dot-layer`-`CircleLayer` (`:84`) und dem `pins-layer`-`SymbolLayer` (`:102`). Beispiel für den letzten:

```kotlin
    SymbolLayer(
        id = "pins-layer",
        source = source,
        minZoom = CLUSTER_MAX_ZOOM.toFloat(),
        visible = visible,
        filter = zoom() gt const(CLUSTER_MAX_ZOOM),
        // … unverändert …
        onClick = onClickPin,
    )
```

(Jeweils `visible = visible` als zusätzliches benanntes Argument einfügen; `visible: Boolean = true` existiert auf `SymbolLayer`/`CircleLayer` in 0.13.0-sc1.)

- [ ] **Step 2: `Map.kt` — Imports**

Hinzufügen:

```kotlin
import androidx.compose.ui.unit.DpOffset
import de.westnordost.streetcomplete.screens.main.map.QuestSelection
import de.westnordost.streetcomplete.screens.main.map2.layers.SelectedPinsLayer
import org.maplibre.compose.util.ClickResult
import org.maplibre.compose.util.FeaturesClickHandler
import org.maplibre.compose.util.MapClickHandler
```

- [ ] **Step 3: `Map.kt` — Signatur erweitern**

Die `Map(...)`-Parameterliste (`:33-43`) um vier Parameter ergänzen (nach `pins` bzw. ans Ende der Klick-Params):

```kotlin
fun Map(
    modifier: Modifier = Modifier,
    cameraState: CameraState = rememberCameraState(),
    styleState: StyleState = rememberStyleState(),
    downloadedTiles: Collection<TilePos> = emptyList(),
    geometryMarkers: Collection<Marker> = emptyList(),
    focusedGeometry: ElementGeometry? = null,
    pins: Collection<Pin> = emptyList(),
    selectedQuest: QuestSelection? = null,
    location: Location? = null,
    rotation: Float? = null,
    onClickPin: FeaturesClickHandler? = null,
    onClickCluster: FeaturesClickHandler? = null,
    onMapClick: MapClickHandler? = null,
) {
```

- [ ] **Step 4: `Map.kt` — `onMapClick` an `MaplibreMap`**

Im `MaplibreMap(...)`-Aufruf (`:45-53`) als Argument ergänzen (z. B. nach `options = …`):

```kotlin
        onMapClick = onMapClick ?: { _, _ -> ClickResult.Pass },
```

- [ ] **Step 5: `Map.kt` — `aboveLabelsContent` umbauen**

Den `aboveLabelsContent`-Block (`:65-70`) ersetzen:

```kotlin
            aboveLabelsContent = {
                GeometryMarkersLayers(geometryMarkers)
                focusedGeometry?.let { FocusedGeometryLayers(it) }
                // Hide normal pins while a quest is selected (Android's hideNonHighlightedPins =
                // setVisible(false)); the selected pin stays on top via SelectedPinsLayer.
                PinsLayers(
                    pins,
                    onClickPin = onClickPin,
                    onClickCluster = onClickCluster,
                    visible = selectedQuest == null,
                )
                selectedQuest?.let { SelectedPinsLayer(it.icon, it.markerLocations) }
                location?.let { CurrentLocationLayers(it, rotation) }
            },
```

- [ ] **Step 6: Link-Gate**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`. (Render-only: noch kein Handler verdrahtet, aber die Composable kompiliert mit neuer Signatur.)

- [ ] **Step 7: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map2/layers/PinsLayers.kt \
        app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map2/Map.kt
git commit -m "M4.3: Map() selection params + SelectedPinsLayer wiring + pins visibility toggle"
```

---

### Task 4: `MapScreen` — Tap-Auswahl + Deselect (erste sichtbare Selektion)

**Files:**
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt`

**Interfaces:**
- Consumes: `MapViewModel.selectedQuest`/`selectQuest`/`clearSelection`/`getQuestKey`; erweiterte `Map(...)` (Task 3); `FeaturesClickHandler`/`MapClickHandler`/`ClickResult`; `Feature`/`JsonObject`/`JsonPrimitive`.
- Produces: gerenderte Selektion bei Tap (Highlight + Umriss + andere Pins weg) und Deselect bei Map-Tap.

- [ ] **Step 1: Imports ergänzen**

```kotlin
import kotlinx.serialization.json.JsonPrimitive
import org.maplibre.compose.util.ClickResult
import org.maplibre.compose.util.FeaturesClickHandler
import org.maplibre.compose.util.MapClickHandler
```

- [ ] **Step 2: `selectedQuest` sammeln**

Bei den anderen `collectAsState()` (`MapScreen.kt:41-49`) ergänzen:

```kotlin
    val selectedQuest by viewModel.selectedQuest.collectAsState()
```

- [ ] **Step 3: Handler bauen** (vor dem `Box {`-Block, `:148`)

```kotlin
    val onClickPin: FeaturesClickHandler = handler@{ features ->
        val props = features.firstOrNull()?.properties.orEmpty()
            .mapNotNull { (k, v) -> (v as? JsonPrimitive)?.let { k to it.content } }
            .toMap()
        val questKey = viewModel.getQuestKey(props)
        if (questKey != null) {
            viewModel.selectQuest(questKey)
            ClickResult.Consume
        } else {
            ClickResult.Pass
        }
    }
    val onMapClick: MapClickHandler = { _, _ ->
        if (viewModel.selectedQuest.value != null) {
            viewModel.clearSelection()
            ClickResult.Consume
        } else {
            ClickResult.Pass
        }
    }
```

- [ ] **Step 4: `Map(...)` füttern**

Im `Map(...)`-Aufruf (`:149-158`) ergänzen — Selektion hat Vorrang vor sonstiger Fokus-Geometrie:

```kotlin
        Map(
            modifier = Modifier.fillMaxSize(),
            cameraState = cameraState,
            downloadedTiles = downloadedTiles,
            geometryMarkers = markers,
            focusedGeometry = selectedQuest?.geometry ?: focusedGeometry,
            pins = pins,
            selectedQuest = selectedQuest,
            location = location,
            rotation = rotation?.let { (it - cameraState.position.bearing.toFloat()) },
            onClickPin = onClickPin,
            onMapClick = onMapClick,
        )
```

(Den `onClickCluster` lässt Task 6 folgen; bis dahin bleibt er ungesetzt = null.)

- [ ] **Step 5: Link-Gate**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 6: Simulator-Verifikation (synthetische Injektion)**

Temporär (zum Screenshot, danach entfernen) in `MapScreen` nach den Handlern einfügen:

```kotlin
    // TEMP M4.3-verify: auto-select the first available pin to screenshot the selection render.
    LaunchedEffect(pins) {
        val p = pins.firstOrNull() ?: return@LaunchedEffect
        viewModel.getQuestKey(p.properties.toMap())?.let { viewModel.selectQuest(it) }
    }
```

Bauen + auf dem Simulator starten (iOS-Cheatsheet im Backlog: `xcodebuild … -sdk iphonesimulator … && xcrun simctl install/launch`; frische DB + heruntergeladenes Gebiet wie bei M4.2, Zoom ≥ 15). **Erwartet im Screenshot:** ein hochskalierter (gebouncter) gewählter Pin, der atmende Geometrie-Umriss, **keine** anderen Pins. Tap auf leere Karte (oder das TEMP-Block durch `clearSelection()` nach Delay ersetzen) → Pins zurück.
Danach den TEMP-Block **entfernen**.

- [ ] **Step 7: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt
git commit -m "M4.3: MapScreen pin-tap select + map-click deselect"
```

---

### Task 5: `MapScreen` — Kamera-Fokus/Restore + Persist-Gate

**Files:**
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt`

**Interfaces:**
- Consumes: `selectedQuest` (Task 4); `cameraState`; `ElementGeometry.bounds: BoundingBox`; `BoundingBox.toGeoJsonBoundingBox()` (`map2.GeometryUtils`); `CameraState.animateTo(boundingBox, bearing, tilt, padding, duration)` und `animateTo(CameraPosition, duration)`.
- Produces: Kamera fokussiert die gewählte Geometrie (mit `focusPadding`) und stellt die Vor-Selektions-Kamera bei Deselect wieder her; Persist überschreibt die Karten-Position nicht mehr während Selektion.

- [ ] **Step 1: Imports ergänzen**

```kotlin
import androidx.compose.foundation.layout.PaddingValues
import de.westnordost.streetcomplete.screens.main.map2.toGeoJsonBoundingBox
import org.maplibre.compose.camera.CameraPosition
```

(`CameraPosition` ist in `MapScreen` evtl. schon importiert — dann nicht doppeln. `LaunchedEffect`, `remember`, `mutableStateOf`, `getValue`, `setValue`, `milliseconds` sind vorhanden.)

- [ ] **Step 2: `focusPadding` + Fokus/Restore-Lifecycle**

Nach den Handlern (Task 4, vor `Box {`) einfügen:

```kotlin
    // Bottom padding the camera reserves below the focused geometry. 0 for now; when the quest-form
    // bottom sheet lands (upstream-blocked), pass PaddingValues(bottom = sheetHeight) here.
    val focusPadding = PaddingValues(bottom = 0.dp)

    // Camera focus on the selected geometry; restore the pre-selection camera on deselect.
    var savedCamera by remember { mutableStateOf<CameraPosition?>(null) }
    LaunchedEffect(selectedQuest) {
        val sel = selectedQuest
        if (sel != null) {
            // capture once per selection CHAIN — not on reselect — so deselect returns to the origin.
            if (savedCamera == null) savedCamera = cameraState.position
            cameraState.animateTo(
                boundingBox = sel.geometry.bounds.toGeoJsonBoundingBox(),
                padding = focusPadding,
                duration = 500.milliseconds,
            )
        } else {
            savedCamera?.let { cameraState.animateTo(it, duration = 500.milliseconds) }
            savedCamera = null
        }
    }
```

- [ ] **Step 3: Persist-Gate ergänzen**

Im bestehenden Persist-`LaunchedEffect` (`MapScreen.kt:71-83`) die Guard-Zeile (`:77`) erweitern, damit der Selektions-Fokus die gemerkte Karten-Position nicht überschreibt:

```kotlin
                if (viewModel.isFollowing.value || viewModel.selectedQuest.value != null) return@collect
```

- [ ] **Step 4: Link-Gate**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`. (Falls `ElementGeometry.bounds` anders heißt — Spec belegt es als `val bounds: BoundingBox` —, hier anpassen.)

- [ ] **Step 5: Simulator-Verifikation**

Mit dem TEMP-Auto-Select aus Task 4 (kurz wieder einsetzen): Screenshot zeigt jetzt zusätzlich, dass die **Kamera auf die Geometrie zentriert/zoomt**. Dann TEMP-Block durch eine verzögerte `clearSelection()` ersetzen → Kamera fährt zur Ausgangsposition zurück, Pins zurück. TEMP-Block wieder entfernen.

- [ ] **Step 6: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt
git commit -m "M4.3: camera focus/restore on selection + persist gate"
```

---

### Task 6: Cluster-Tap → Fallback-Zoom

**Files:**
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt`

**Interfaces:**
- Consumes: `onClickCluster: FeaturesClickHandler`-Slot von `Map(...)` (Task 3); `cameraState.position.zoom`; `org.maplibre.spatialk.geojson.Point`; `CameraPosition`; `rememberCoroutineScope`.
- Produces: Tap auf einen Cluster zoomt auf den Cluster-Punkt rein (> `CLUSTER_MAX_ZOOM`=14), sodass der Cluster in Einzel-Pins zerfällt.

- [ ] **Step 1: Imports ergänzen**

```kotlin
import androidx.compose.runtime.rememberCoroutineScope
import kotlinx.coroutines.launch
import org.maplibre.spatialk.geojson.Point
```

- [ ] **Step 2: Scope + Cluster-Handler**

Bei den Handlern (Task 4) ergänzen:

```kotlin
    val scope = rememberCoroutineScope()
    // No cluster-leaf query API on iOS (maplibre-compose 0.13.0 has no getClusterLeaves /
    // querySourceFeatures), so we cannot fit the leaves' bbox like Android's zoomToCluster. Fallback:
    // zoom in toward the cluster point past CLUSTER_MAX_ZOOM (=14), which dissolves it into pins.
    val onClickCluster: FeaturesClickHandler = handler@{ features ->
        val point = features.firstOrNull()?.geometry as? Point ?: return@handler ClickResult.Pass
        val zoom = minOf(cameraState.position.zoom + 2.0, 15.0)
        scope.launch {
            cameraState.animateTo(CameraPosition(target = point.coordinates, zoom = zoom))
        }
        ClickResult.Consume
    }
```

- [ ] **Step 3: `onClickCluster` an `Map(...)` übergeben**

Im `Map(...)`-Aufruf ergänzen:

```kotlin
            onClickCluster = onClickCluster,
```

- [ ] **Step 4: Link-Gate**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`. (Falls `Point.coordinates` anders heißt — spatialk-`Point` —, hier korrigieren.)

- [ ] **Step 5: Simulator-Verifikation**

Auf dem Simulator auf Zoom 13–14 herauszoomen (Cluster sichtbar). Da ein scriptbarer Tap auf den Cluster schwierig ist, alternativ den `onClickCluster`-Block temporär auf den ersten Cluster-Feature anwenden bzw. die Geräte-Verifikation (Task 7) als maßgeblich nehmen. Erwartet: nach „Cluster-Tap" zoomt die Karte rein und der Cluster zerfällt in Einzel-Pins.

- [ ] **Step 6: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt
git commit -m "M4.3: cluster-tap fallback zoom (no leaf-query API on iOS)"
```

---

### Task 7: Seam-Marker, Padding-Audit & Gesamt-Verifikation

**Files:**
- Modify: `app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt` (nur Kommentar)

**Interfaces:**
- Consumes: alles aus Task 1–6.
- Produces: dokumentierte Naht fürs künftige Formular; bestätigte Gesamt-Verifikation (Link-Gate + Simulator + Gerät).

- [ ] **Step 1: Seam-Marker setzen**

Im `Box`-Overlay von `MapScreen` (bei den Controls) einen Kommentar setzen — **ohne** den `TODO()`-Commit-Pfad anzufassen:

```kotlin
        // TODO(M4.x quest form): when the upstream compose-quest-form path unblocks (OsmQuestFormContainer
        //  commit handlers are currently all TODO()), mount it in a ModalBottomSheet here, driven by
        //  `selectedQuest`, and pass the sheet height into `focusPadding` above so the geometry sits
        //  above the sheet (= Android getQuestFormInsets()). No selection/highlight code changes needed.
```

- [ ] **Step 2: Padding-Klemmen-Audit**

Verifizieren, dass **kein** in M4.3 berührter Layer ein negatives `iconPadding`/`PaddingValues.Absolute` einführt (`SelectedPinsLayer`/`PinsLayers` sind bereits ≥0 geklemmt; M4.3 fügt keine neuen Icon-Padding-Werte hinzu). Reiner Review-Schritt; keine Codeänderung erwartet.

- [ ] **Step 3: Link-Gate (final)**

Run: `./gradlew :app:linkDebugFrameworkIosSimulatorArm64`
Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 4: Simulator-Gesamtlauf (Screenshots)**

Bauen + starten (frische DB, heruntergeladenes Gebiet, Zoom ≥ 15). Die fünf Szenarien aus dem Spec belegen (via synthetischer Injektion wo Tap nicht scriptbar): (1) Einzel-Pin → Highlight + Umriss + andere Pins weg + Kamerafokus; (2) Deselect → alles zurück + Kamera restored; (3) Cluster → zerfällt; (4) A→B-Reselect → wandert; (5) Persist: nach Selektion+Deselect bleibt die ursprüngliche Karten-Position erhalten.

- [ ] **Step 5: Geräte-Verifikation (echter Tap)**

Aufs iPhone 16 Pro deployen (Geräte-Cheatsheet; `-allowProvisioningUpdates`). **Echte Finger-Taps** bestätigen das Hit-Testing/`ClickResult`-Routing, das im Simulator nicht scriptbar ist: Pin antippen → Auswahl; daneben → Deselect; Cluster → Zoom. (Interaktiv; ggf. Piotr.)

- [ ] **Step 6: Commit**

```bash
git add app/src/commonMain/kotlin/de/westnordost/streetcomplete/screens/main/map/MapScreen.kt
git commit -m "M4.3: quest-form seam marker + final verification"
```

---

## Self-Review

**Spec-Coverage (jede Spec-Sektion → Task):**
- Tap→select / decode / load (§Lieferumfang 1–3) → Task 2 (`selectQuest`) + Task 4 (Handler/decode). ✓
- Highlight gewählter Pin (4) → Task 3 (`SelectedPinsLayer` in `Map.kt`) + Task 2 (`icon`). ✓
- Geometrie-Umriss (5) → Task 4 (`focusedGeometry = selectedQuest?.geometry ?: …`). ✓
- Kamerafokus + Restore (6) → Task 5. ✓
- Andere Pins ausblenden (7) → Task 3 (`visible = selectedQuest == null`). ✓
- Cluster-Tap-Fallback (8) → Task 6. ✓
- Deselect (9) → Task 4 (`onMapClick`) + Task 5 (Restore). ✓
- Naht fürs Formular → Task 7 (Marker). ✓
- Architektur: State in `MapViewModel`/`@Stable`-Holder → Task 1+2. ✓
- Reconciliations: ClickResult-Disziplin (Task 4/6), Persist-Gate (Task 5), Threading (Task 2, `Dispatchers.IO`), Padding-Klemmen (Task 7), Cluster-Fallback (Task 6). ✓
- Verifikation: Link-Gate jeder Task + Simulator (4,5,6,7) + Gerät (7). ✓

**Placeholder-Scan:** kein TBD/TODO-als-Platzhalter; die einzigen `TODO`s sind der bewusste Naht-Kommentar (Task 7) und der TEMP-Verify-Block (explizit zu entfernen). ✓

**Typ-Konsistenz:** `QuestSelection(questKey, icon: String, markerLocations: List<LatLon>, geometry: ElementGeometry)` einheitlich in Task 1/2/3/5; `selectQuest(QuestKey)`/`clearSelection()`/`selectedQuest: StateFlow<QuestSelection?>` einheitlich Task 2→4→5; `Map(... selectedQuest, onClickPin, onClickCluster, onMapClick)` einheitlich Task 3→4→6; `PinsLayers(..., visible)` Task 3. ✓

**Risiko-Hinweise für den Implementierer:** drei API-Namen sind aus der Spec/Investigation belegt, aber beim ersten Compile zu bestätigen: `VisibleQuestsSource.get(QuestKey): Quest?`, `ElementGeometry.bounds: BoundingBox`, `Point.coordinates: Position`. Bei Abweichung lokal anpassen (Link-Gate zeigt es sofort).
